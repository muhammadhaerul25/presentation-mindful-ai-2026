const { Pool } = require('pg'); const pool = new Pool({connectionString: 'postgresql://postgres.xtcfptlwbzgrdxpfkcux:muhammadhaerul2512@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres', ssl: { rejectUnauthorized: false }}); async function run() { try { const query = 'CREATE TABLE IF NOT EXISTS feedbacks (id SERIAL PRIMARY KEY, nama VARCHAR(255), email VARCHAR(255), rating INTEGER, alasan TEXT, pesan TEXT, source VARCHAR(255), created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP);'; await pool.query(query); console.log('created'); } catch(e) { console.error(e); } finally { pool.end(); } } run();
